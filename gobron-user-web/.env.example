# Backend API base URL
VITE_API_URL=https://gobronapi.webportfolio.uz/api/v1

# Browser dev fallback login (used only when NOT inside Telegram).
# The backend accepts OTP_DEV_CODE as the master code in dev mode.
VITE_DEV_PHONE=+998900000000
VITE_DEV_OTP=111111
